{
  "name": "parcel-example",
  "version": "1.0.0",
  "description": "An example using parcel instead of webpack",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html"
  },
  "dependencies": {
    "session-typed-worker": "^1.0.2"
  },
  "devDependencies": {
    "parcel-bundler": "^1.10.3",
    "typescript": "^3.2.2"
  }
}
